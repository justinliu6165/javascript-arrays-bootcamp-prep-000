var chocolateBars = ['snickers','hundred grand', 'kitkat', 'skittles'];

function addElementToBeginningOfArray(arr){
  var array = "";
  for (i=0; i<=arr.length; i++){
    arr.unshift(array[i]);
  }
  return array;
}

function destructivelyAddElementToBeginningOfArray(arr) {
  
}
